@using MusicOrganizer.Models;

@if (Model.Count == 0)
{
  <p>There are no genres of music in your library!</p>
}
<ul>
  @foreach (Genre genre in Model)
  {
    <li><a href=/genres/@genre.Id>@genre.Type</a></li>
  } 
</ul>